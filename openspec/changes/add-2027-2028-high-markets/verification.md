# 验收 · 2026-09-05

本次需求 7/7 项完成。2028 无可验证合约是明确的数据覆盖限制；四档 UI 与对应年份 API/子站均已实现，不外推未知概率。

| 范围 | 结果 |
| --- | --- |
| 主站单元测试 | 46 passed |
| 主站浏览器 | 22 场景、6 宽度，无失败 |
| 主站语法与 OpenSpec | 10 文件语法检查、strict validate 通过 |
| probs Worker | 50 单元测试通过，部署构建通过 |
| probs 浏览器 | 两年份 × 四宽度（320/375/768/1280），四档及中英文/2026 导航通过 |
| 真实生产回读 | fuckbtc.com、btc.x.fish 与 probs 两年页面/API 均通过，无 pageerror / 横向溢出 |

完整性：四档按 $100k / $150k / $200k / $250k 排列；2027 两个真实报价，其余显示未知；2028 四档未知且明确无合约。默认 1.5% 仍推出 22.5 万美元情景目标价，概率不参与资金推导。

正确性：年份、个体截止、ID、slug、Yes 下标、概率范围与源时间经过校验。测试覆盖错误年份、未来时间、过期缓存、慢请求正文、缺报价、年度隔离、缓存恢复不续期。子站不把稀疏数据拟合成预测最高价。

一致性：复用现有调度、KV、Cloudflare 部署，2027 旧 API/导出保留，2026 原有行情与算法未改变，仅增加年份导航。无需新秘密或基础设施。

功能发布：主站提交 `ea5df65`，Pages 部署 `b22e1c0c-a660-4b14-87ba-36889eef49dd`；probs 源码提交 `ed5e6b6`，Worker 版本 `fa28edfd-b302-4c71-98dc-339ac564b542`。本记录随后以文档提交同步，不改变功能版本。
